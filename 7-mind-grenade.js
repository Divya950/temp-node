const num1=5
const num2=10
function addValues(){
    console.log(`the added value is : ${num1+num2}`)
}
addValues:void
addValues()

// requiring means invoking the code if we have the function is excuted in the code

